# DreamZero-DROID deploy: three disaggregated diffusion stages.
#
#   Stage 0  Encode   -> tokenizer + UMT5 + CLIP + Wan VAE encode
#   Stage 1  Denoise  -> CausalWan DiT + AR-Diffusion paged KV, TP=4
#   Stage 2  Decode   -> action postprocess
#
# Topology (stage roles and payload keys) is declared in
# vllm_omni/model_executor/models/dreamzero/pipeline.py. This file owns only
# runtime placement and transport.
#
# The transport is named here and nowhere else: DreamZero's model code declares
# the payload keys and never a connector. Moving this deployment to a different
# transport is a change to the two `connectors:` entries below (name, plus any
# backend-specific `extra`) and nothing else -- no DreamZero source, no stage
# dispatch, no payload schema, and no change to the XPU tensor-parallel
# collectives Stage 1 uses.
#
# Both edges carry the same payload key (`dreamzero_stage_payload`); the payload's
# own `boundary` field distinguishes encode->denoise from denoise->decode.

pipeline: dreamzero_disaggregated
async_chunk: false
distributed_executor_backend: mp
dtype: bfloat16

connectors:
  encode_payload:
    name: SharedMemoryConnector
  denoise_payload:
    name: SharedMemoryConnector

stages:
  # ---- Stage 0: Encode ----------------------------------------------------
  # Runs the encoders only; no DiT, no AR-Diffusion engine, no KV pool.
  - stage_id: 0
    devices: "4"
    max_num_seqs: 1
    enforce_eager: false
    model_class_name: DreamZeroPipeline
    model_config:
      default_robot_embodiment: roboarena
      policy_server_config:
        image_resolution: [180, 320]
        n_external_cameras: 2
        needs_wrist_camera: true
        needs_stereo_camera: false
        needs_session_id: true
        action_space: joint_position
    output_connectors:
      to_stage_1: encode_payload

  # ---- Stage 1: Denoise ---------------------------------------------------
  # The four logical devices form one vLLM tensor-parallel group, so the sharded
  # CausalWan DiT keeps using vllm.distributed collectives (XPU xccl, falling
  # back to oneCCL) exactly as it does in the monolithic deployment. XPU device
  # visibility is still remapped by ZE_AFFINITY_MASK.
  #
  # Disaggregation does not touch that path: the connector only delivers the
  # stage payload to the transfer-leader rank, which broadcasts it over the same
  # TP group; every rank then runs its own shard of the DiT forward.
  - stage_id: 1
    devices: "0,1,2,3"
    max_num_seqs: 1
    enforce_eager: false
    model_class_name: DreamZeroPipeline
    # DreamZero's denoise stage runs on the AR-Diffusion engine (engine-level
    # paged KV cache). AR-Diffusion KV exists only on this stage.
    engine_backend: vllm_omni.experimental.ar_diffusion.engine.ARDiffusionEngine
    parallel_config:
      tensor_parallel_size: 4
      cfg_parallel_size: 1
    model_config:
      default_robot_embodiment: roboarena
      policy_server_config:
        image_resolution: [180, 320]
        n_external_cameras: 2
        needs_wrist_camera: true
        needs_stereo_camera: false
        needs_session_id: true
        action_space: joint_position
    cache_backend: step_cache
    cache_config:
      step_cache_dit_enabled: true
      velocity_sim_thresholds: [0.95, 0.93]
      velocity_skip_countdowns: [4, 2]
    input_connectors:
      from_stage_0: encode_payload
    output_connectors:
      to_stage_2: denoise_payload

  # ---- Stage 2: Decode ----------------------------------------------------
  # Action postprocess only. DreamZero's response carries normalized video
  # latents plus actions, so this stage loads no VAE decoder and no weights.
  - stage_id: 2
    devices: "4"
    max_num_seqs: 1
    enforce_eager: true
    model_class_name: DreamZeroPipeline
    model_config:
      default_robot_embodiment: roboarena
      policy_server_config:
        image_resolution: [180, 320]
        n_external_cameras: 2
        needs_wrist_camera: true
        needs_stereo_camera: false
        needs_session_id: true
        action_space: joint_position
    input_connectors:
      from_stage_1: denoise_payload
